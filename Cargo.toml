[workspace]
members = ["src/libkrun", "src/krun_input", "krun-sys-windows"]
exclude = ["examples/gtk_display"]
resolver = "2"

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true

[patch.crates-io]
vm-memory = { path = "third_party/vm-memory" }
